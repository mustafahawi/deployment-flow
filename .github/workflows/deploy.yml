on:
  workflow_call:
    inputs:
      env:
        type: string
        required: true

jobs:
  build:
    name: Deploy
    runs-on: ubuntu-latest
    environment: ${{inputs.env}}
    steps:      
      - name: Deploy
        run: echo "${{secrets.env_name}}"
